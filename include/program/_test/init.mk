. := $(realpath $(dir $(lastword $(MAKEFILE_LIST))))

#---------------------------------------------------------------------------------------------------

$(.)/.build:
	$(call mkdir,$@)

$(c++-compilers:%=$(.)/.build/%): %: | $(.)/.build
	$(call mkdir,$@)

#---------------------------------------------------------------------------------------------------
